int main() {

   
    int i,n,s=0;
    scanf("%d",&n);
   int *a=(int*)malloc(n*sizeof(int));
    for(i=0;i<n;i++)
    {
        scanf("%d",& a[i]);
      
    s=s+a[i];
    }
    printf("%d",s);
    free(a);
    return 0;
}
