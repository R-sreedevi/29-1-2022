#include<stdio.h>
int fact(int);
int main()
{
   int n,f;  
   printf("Enter any number");
   scanf("%d",&n);    
   f =fact(n);   
   printf("factorial of %d is: %d",n,f);
   return 0;
}
int fact(int m)
{   if(m==0)
    return(1);
   return(m*fact(m-1));
}
